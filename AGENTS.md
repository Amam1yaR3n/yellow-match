# 项目执行约束

## 测试约束

- 本项目不编写、不生成、不运行任何单元测试、集成测试、端到端测试或其他自动化测试。
- 不创建测试目录、测试文件、测试脚本、测试依赖、测试配置、独立测试清单或测试报告。
- 不引入 Vitest、Jest、Playwright、Cypress 等测试框架。
- 游戏试玩、功能验收、兼容性检查和回归测试全部由用户自行完成，代理不得代替用户执行。
- 允许执行 `pnpm typecheck` 和 `pnpm build`，仅用于确认 TypeScript 类型检查和生产构建能够完成，不视为测试。
- 计划中的功能完成标准只供用户验收参考，代理不得自行执行这些场景。
- 除非用户后续明确修改本文件，不得新增测试或改变以上约束。

## 美术素材约束

- 需要透明背景的素材，优先尝试生成透明背景；若未成功，不因背景问题重试，直接交给用户抠图，代理不自行抠图。

## 资产引用约束

- 项目源码与生产构建产物中的本地 JavaScript、CSS、图片、字体、音频、视频等静态资产必须使用相对路径引用，不得使用以 `/` 开头的站点根绝对路径、`file://` 路径或本机绝对路径。
- Vite 的 `base` 必须保持为 `"./"`，并保持 `assetsInlineLimit: 0`，不得将本地资产内联为 `data:` 或 `blob:` URL。
- 哔哩哔哩 Toy 上传包必须由生产构建产物生成，`index.html` 位于压缩包根目录；不得打入源码、`node_modules`、`.git`、`.DS_Store`、`__MACOSX` 或 `toy.yaml`。

## Git 提交说明约束

- Git 提交标题、提交正文、变更摘要以及提交或合并请求中的修改说明必须使用简体中文。
- 文件路径、代码标识符、命令、依赖名称和无法准确翻译的专有名词可以保留原文。
